# Google Maps API key (required for the photorealistic 3D map).
# 1. Create a key in the Google Cloud console (billing enabled):
#    https://console.cloud.google.com/google/maps-apis
# 2. Enable "Maps JavaScript API", "Map Tiles API" and "Directions API".
# 3. Copy this file to `.env` and paste your key below.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# Optional: Gemini API key for LLM-phrased rescue-assistant chat replies.
# Without it the assistant uses built-in deterministic replies (demo-safe).
# Create at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=

