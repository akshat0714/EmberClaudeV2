# Mapbox access token (required for the 3D map).
# Create a free token at https://account.mapbox.com/access-tokens/
# then copy this file to `.env` and paste your token below.
VITE_MAPBOX_TOKEN=your_token_here
